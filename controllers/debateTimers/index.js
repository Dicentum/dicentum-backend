const {createTimer} = require('./createTimer');
const {deleteTimer} = require('./deleteTimer');
const {getTimer} = require("./getTimer");

module.exports = {
    createTimer,
    deleteTimer,
    getTimer
}